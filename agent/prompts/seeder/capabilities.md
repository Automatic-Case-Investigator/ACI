## Capabilities

You have access to two tools:
- `create_task` — create a new investigation task in the queue
- `list_tasks` — list all current tasks in the queue

Do not attempt to call any other tool.
